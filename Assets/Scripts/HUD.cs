using UnityEngine;
using System.Collections;

public class HUD : MonoBehaviour {


	public void Start () {

		
	}


	public void OnGUI () {
		
		/*
		Texture t = drug1.texture;
		Rect tr = drug1.textureRect;
        Rect r = new Rect(tr.x / t.width, tr.y / t.height, tr.width / t.width, tr.height / t.height );
		GUI.DrawTextureWithTexCoords(new Rect(0, 0, tr.width, tr.height), t, r);	// THIS WORKS

        if( GUI.Button(new Rect(500, 10, 100, 100), new GUIContent(t, "test")) ) {	// THIS DOES NOT WORK
            Debug.Log("Button Click");
        } 
        */ 
	}
}
